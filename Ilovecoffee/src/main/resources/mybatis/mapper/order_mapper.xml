<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ilove.coffee.order.OrderDAO">

	<insert id="order" parameterType="hashMap">
		insert into myorder(customerId, totalPrice, deliveryStatus, zipcode, address, addressDetail)
		values(#{customerId}, #{totalPrice}, #{deliveryStatus}, #{zipcode}, #{address}, #{addressDetail})
	</insert>
   	
   	<delete id="deleteOrder" parameterType="int">
   		delete myorder
   		where orderNo=#{orderNo}
   	</delete>
   	
</mapper>